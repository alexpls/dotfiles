# macOS indexing file, haven't ever needed to commit this to a repo
.DS_Store

# Used across repos for common helper scripts that I don't want to commit
.x

# Ignore jujutsu dir on existing git projects
.jj

# Ignore nvim files per-project config (exrc)
.nvim.lua

# Ignore aider dirs
.aider*
